---
description: Creating an EKS Worker Node by adding an EC2 Host in DuploCloud
---

# 5. Create an EKS Worker Node

To create an EKS Worker Node, navigate to **Cloud Services** -> **Hosts**, and click **Add**. The **Add Host** page displays. Enter the following details:

**Friendly Name**: `host01`&#x20;

{% hint style="info" %}
If you click **Advanced Options**, you’ll see that the **Agent Platform** field is automatically set to **EKS Linux** for this new node.
{% endhint %}

<figure><img src="../../../.gitbook/assets/host red.png" alt=""><figcaption><p>The <strong>Add Host</strong> page</p></figcaption></figure>

Click **Add**. It may take a few minutes for the Worker Node to be complete. Wait until the **Status** displays `Running` and **Fleet** displays `Connected`.
